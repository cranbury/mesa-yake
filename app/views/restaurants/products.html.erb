<div class="container">
    <div class="row">
        <div class="col pt-2">
            <% @products.each do |product|  %>
                <a href="/restaurants/<%= @restaurant_id %>/product/<%= product.id %>">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title"><%= product.name %></h4>
                            <div class="card-body">
                                
                            </div>
                        </div>
                    </div>
                </a>
            <% end %>
        </div>
    </div>
</div>